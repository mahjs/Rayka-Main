import HeroSection from "@/components/HeroSection";
import React from "react";

const Jobs: React.FC = () => {
  return (
    <div>
      <HeroSection
        heading="Find your dream job"
        subHeading="Search for jobs in your area"
      />
    </div>
  );
};

export default Jobs;
